"""Human approval queue: storage for drafts/decisions and a small server-rendered web UI.

Named approval_queue (not queue) because a top-level package called ``queue`` shadows the
standard-library module of the same name.
"""
